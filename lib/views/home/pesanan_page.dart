import 'package:flutter/material.dart';

class PesananPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Pesanan Page"),
      ),
    );
  }
}
